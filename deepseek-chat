#!/bin/bash
# Wrapper pour lancer DeepSeek Chat CLI

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# Activer venv si existe
if [ -d "venv" ]; then
    source venv/bin/activate
fi

# Lancer le chat
python3 scripts/deepseek_chat.py "$@"
