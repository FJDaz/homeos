# API Configuration — used for GET /studio/genome and Sullivan routes.
# En dev (npm run dev), laisser vide ou /api : le proxy Vite envoie /api/* vers http://127.0.0.1:8000.
# Pour que /studio affiche les organes : lancer l'API (./start_api.sh à la racine) puis le frontend.
# PUBLIC_API_URL=http://127.0.0.1:8000