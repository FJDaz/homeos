#!/bin/bash
# Raccourci AetherFlow pour Groq R1-Distill
PROJECT_DIR="/Users/francois-jeandazin/AETHERFLOW"
cd "$PROJECT_DIR"
if [ -d "venv" ]; then source venv/bin/activate; fi
python3 groq-r1.py "$@"
